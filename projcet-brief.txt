Classes:

Product — has a name, price, stock quantity, category
User — has a username, password, and a role (customer vs admin)
Cart — belongs to a user, holds items + quantities
Order — a "snapshot" of a completed cart with a status and timestamp
Store — the main engine that manages everything (products, users, orders)

Build Order:
Phase 1 — Products & Admin
→ Add, update, remove, and list products. Nothing fancy yet, just get OOP working.

Phase 2 — Users & Auth
→ Register, login, logout. Store passwords hashed (look up the hashlib or bcrypt module — great for your portfolio).
→ User Class attributes:
  -user_id
  -user_name
  -password
  -role

→ User Class methods:
  - check_password()
  - change_password()
  -is_Admin()

Note:
-> Methods shouldn't print() — that's the CLI's job.

Phase 3 — Shopping (Cart)
→ Logged-in users can browse, add to cart, remove from cart, see totals.

# constructor:
Cart is created → empty
    ↓
user calls add_item(product, quantity) → item added to dictionary
    ↓
user calls add_item(product, quantity) → another item added
    ↓
cart now has multiple items

→ Cart class methods:
  - add_item()
  - remove_item()
  - update_quantity()
  - calculate_total()
  - view_cart()
  - checkout():
       # 1. Copy the existing cart
       # 2. Loop through cart items and reduce stock for each product
       # 3. Clear the cart after
       # 4. Return something useful (i.e ordered_items)

Phase 4 — Orders
→ Cart becomes an Order. Users can view their order history.

Order.py
Attributes:
  - order_id
  - Who placed the order? (user_id)
  - What was ordered? (comes from checkout())
  - What's the total price?
  - When was it placed? (datetime.now())
  - What's the status? (e.g. "pending", "delivered")

For status — give it a default value in __init__ so the caller doesn't have to pass it every time. \

Phase 5 — Store.py 
→ store.py is where everything comes together.
→ Think about it as the manager of the whole system. It holds all the data and coordinates between classes.

Store
├── manages → {products}  (dict of Product objects)
├── manages → {users}     (dict of User objects)
└── manages → {orders}    (dict of Order objects)


→ For attributes — what collections does a store need to keep track of?
  Think about what a real store manages:
    → Store name
    → All the products available
    → All registered users
    → All placed orders

→ Users dictionary ✅
    user_id as key, User object as value — clean and fast lookup.
→ Orders dictionary ✅
    order_id as key, Order object as value — same logic, perfect.
→ Products ✅
    A simple dictionary like users/orders would work fine here too:
    product_id as key, Product object as value.


→ For methods:

--> Product management methods:
   - add_product() first — the store receives product details and creates a Product object and adds it to self.products.
          # Arguments — the store should receive the raw details and create the Product itself.  
           - So arguments should be product_id, name, price, stock and inside the method you create Product        (product_id, name, price, stock).

           Duplicate check — what if a product with the same id already exists? 🤔

  - remove_product()

  - update_product() — an admin might want to update the name, price, or stock of an existing product.
        — what if the admin only wants to update the price but not the name or stock? 
        - You don't want to force them to pass all three values every time.
        - the clean approach is default parameters set to None:
            def update_product(self, id, name=None, price=None, stock=None):

--> User Mangement methods:
    - register_user() — creates a new user and adds to self.users
    - login_user() — finds the user and verifies their password

--> Order management methods:
    - place_order() — creates an Order object from a checked-out cart and adds it to self.orders
                    - The cart already exists somewhere — the customer has been adding items to it before checkout. So `place_order()` should **receive** the cart as an argument, not create a new one.

                    customer adds items to cart (in main.py)
                            ↓
                    customer says "checkout"
                            ↓
                    place_order(user_id, order_id, cart) ← cart is passed in
                            ↓
                    cart.checkout() is called here

    
    - get_user_orders() — takes a user_id and returns all orders belonging to that user 
                        - (hint: loop through self.orders and filter by user_id)
                        - A user can have multiple orders, so you shouldn't return inside the loop (that stops after the first match).
                        - Instead, build a list of matching orders and return it at the end. 


    - update_order_status() — admin updates the status of an order
                            - order_id to find the order
                            - new_status to pass along


Phase 6:  Storage
→ Save everything to a SQLite database so data persists. (SQLite with Python's built-in sqlite3 module)

We'll need 3 tables:
  - products — stores product data
        products → id, name, price, stock 

  - users — stores user data
        id TEXT
        name TEXT
        password TEXT
        role TEXT           

  - orders — stores order data
        id TEXT
        user_id TEXT
        ordered_items → TEXT (JSON string)
        status → TEXT
        ordered_at → TEXT
        total → REAL

database.py will have these responsibilities:
    - Connect to the database and create tables if they don't exist
                Start with create_tables() — this function should:
                1. Connect to store.db
                2. Create a cursor
                3. Create your 3 tables with the right columns
                4. Commit and close

    - save_products() needs to do:
            Receive the products dictionary from Store
            Connect to the database
            Loop through each product and INSERT it into the table
            Commit and close


    - load_products() — this is the reverse. It should:
            Connect to the database
            SELECT * FROM products
            Loop through the results and create a Product object for each row
            Return a dictionary of {id: Product object}



Phase 7: main.py

program starts
      ↓
create tables + load data into Store
      ↓
show main menu (login / register / exit)
      ↓
user logs in
      ↓
show either ADMIN menu or CUSTOMER menu
      ↓
user does stuff
      ↓
save data before exiting


if __name__ == "__main__":
        put your startup code inside it:

        Create tables
        Load data into Store
        Call main_menu()


main_menu():
 main menu should show options like:

    Login:
        - Ask for id and password
        - Call store.login_user(id, password) — it returns the user object
        - Handle wrong credentials with try/except
        - If successful, check user.is_admin() and call either admin_menu(store, user) 
        or customer_menu(store, user)

    Register:
        When the user chooses Register, you need to:
            - Ask for id, name, password, and role
            - Call my_store.register_users()
            - Handle any exceptions (duplicate user etc.)
    Exit


admin_menu(store, user). An admin can:

    Add product
    Remove product
    Update product
    View all products
    Update order status
    View all orders
    Logout